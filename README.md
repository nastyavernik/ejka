# Автотесты авторизации и создания проекта для Ёжка

## Установка

```bash
python -m venv .venv
source .venv/bin/activate  # или .venv\Scripts\activate
pip install -r requirements.txt
playwright install
